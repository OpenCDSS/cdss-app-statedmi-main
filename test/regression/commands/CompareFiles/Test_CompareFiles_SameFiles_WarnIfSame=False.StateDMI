# Test comparing the same files but do not generate a warning when the files are the same
StartLog(LogFile="Results/Test_CompareFiles_SameFiles_WarnIfSame=False.StateDMI.log")
CompareFiles(InputFile1="Data/A1.txt",InputFile2="Data/A1.txt",WarnIfSame=False)
